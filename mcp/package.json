{
  "name": "henro-mcp-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server that lets agents read and control a running Henro canvas over a local WebSocket bridge.",
  "bin": {
    "henro-mcp-bridge": "./src/server.js"
  },
  "scripts": {
    "start": "node src/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "ws": "^8.21.0",
    "zod": "^3.24.0"
  }
}
